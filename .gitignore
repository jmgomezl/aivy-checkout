# internal build prompts / working notes — not part of the product
FABLE_PROMPT.md

# secrets — never commit keys
.env
.env.*
.secret
.secret.*

# foundry build artifacts
cache/
out/

# node
offchain/node_modules/
webapp/node_modules/

# build output — rebuilt by scripts/deploy.sh on every deploy
webapp/dist/

# relayer file-fallback receipts (regenerated per run)
offchain/receipts/

# evidence blobs from local runs — content-addressed, regenerated, and not ours to publish
offchain/evidence/
