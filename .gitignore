# secrets — never commit keys
.env
.env.*
.secret
.secret.*

# foundry build artifacts
cache/
out/

# node
offchain/node_modules/

# relayer file-fallback receipts (regenerated per run)
offchain/receipts/
